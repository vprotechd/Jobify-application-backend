import React,{useEffect,useState}from"react";import{ScrollView,Text,View}from"react-native";import api from"../services/api";import{styles,Header,Loader,Empty}from"../components/common";import{colors}from"../theme/theme";
export default function CandidateApplications(){const[a,setA]=useState([]),[loading,setLoading]=useState(true);useEffect(()=>{(async()=>{try{const r=await api.get("/applications/candidate/applications");setA(r.data.applications||r.data.data||[])}catch{}finally{setLoading(false)}})()},[]);return <ScrollView style={styles.screen} contentContainerStyle={styles.content}><Header title="My applications" subtitle="Track every application you have submitted."/>{loading?<Loader/>:a.length?a.map(x=><View style={styles.card} key={x._id}><Text style={{fontSize:17,fontWeight:"800"}}>{x.job?.title||x.jobTitle||"Job application"}</Text><Text style={styles.muted}>{x.company?.name||x.job?.company?.name||"Company"}</Text><Text style={{marginTop:8,color:colors.primary,fontWeight:"700"}}>{x.status||"pending"}</Text></View>):<Empty text="You haven't applied to any jobs yet."/>}</ScrollView>}
