import React,{useEffect,useState}from"react";import{ScrollView,Text,TouchableOpacity}from"react-native";import api from"../services/api";import{styles,Header,Loader,Empty}from"../components/common";export default function Companies({navigation}){const[c,setC]=useState([]),[loading,setLoading]=useState(true);useEffect(()=>{(async()=>{try{const r=await api.get("/companies");setC(r.data.companies||r.data.data||[])}catch{}finally{setLoading(false)}})()},[]);return <ScrollView style={styles.screen} contentContainerStyle={styles.content}><Header title="Companies" subtitle="Explore companies hiring on Jobify."/>{loading?<Loader/>:c.length?c.map(x=><TouchableOpacity style={styles.card} key={x._id} onPress={()=>navigation.navigate("CompanyDetails",{id:x._id,company:x})}><Text style={{fontSize:18,fontWeight:"800"}}>{x.name}</Text><Text style={styles.muted}>{x.location||x.industry||"Company"}</Text></TouchableOpacity>):<Empty text="No companies found."/>}</ScrollView>}
